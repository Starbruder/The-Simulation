<Window x:Class="TheSimulation.EvaluationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:oxy="http://oxyplot.org/wpf"
        Title="Forest fire simulation evaluation"
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
		ResizeMode="CanMinimize">

	<Window.Resources>
		<Style x:Key="CustomButtonStyle" TargetType="Button">
			<Setter Property="Background" Value="#FF4C8F94"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="Padding" Value="10,5"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Height" Value="40"/>
			<Setter Property="Width" Value="140"/>
			<Setter Property="HorizontalAlignment" Value="Right"/>
			<Setter Property="VerticalAlignment" Value="Top"/>
		</Style>
	</Window.Resources>

	<Grid Background="White" Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
			<TextBlock Text="Simulation Evalualtion" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

			<TextBlock x:Name="EvalHumidity" FontSize="16" Text="Air humidity: 0%"/>
			<TextBlock x:Name="EvalTemperature" FontSize="16" Text="Air temperature: 0 °C"/>
			<!-- Abstand -->
			<Rectangle Height="5"/>
			<TextBlock x:Name="EvalAverageWindDirection" FontSize="16" Text="Average Wind Direction: 0°"/>
			<TextBlock x:Name="EvalAverageWindSpeed" FontSize="16" Text="Average Wind Speed: 0% (0 Bft)"/>
			<!-- Abstand -->
			<Rectangle Height="5"/>
			<TextBlock x:Name="EvalRuntime" FontSize="16" Text="Runtime: 00:00:00"/>
		</StackPanel>

		<Button Grid.Row="0"
			HorizontalAlignment="Right"
			VerticalAlignment="Top"
			Click="ExportCsv_Click"
			Style="{StaticResource CustomButtonStyle}"
			Content="Export CSV"/>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<!-- Chart 1: Grown / Burned -->
			<Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="5">
				<oxy:PlotView x:Name="GrownBurnedPlot"/>
			</Border>

			<!-- Chart 2: Active Trees -->
			<Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
				<oxy:PlotView x:Name="ActiveTreesPlot"/>
			</Border>
		</Grid>
	</Grid>
</Window>
