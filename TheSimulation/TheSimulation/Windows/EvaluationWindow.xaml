<Window x:Class="TheSimulation.EvaluationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:oxy="http://oxyplot.org/wpf"
		xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Forest fire simulation evaluation"
        Height="700" Width="1000"
		ResizeMode="CanMinimize">

	<Grid Background="White" Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
			<TextBlock Text="Simulation Evalualtion" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

			<TextBlock Text="{Binding HumidityText}" FontSize="16"/>
			<TextBlock Text="{Binding TemperatureText}" FontSize="16"/>
			<!-- Abstand -->
			<Rectangle Height="5"/>
			<TextBlock Text="{Binding WindSpeedText}" FontSize="16"/>
			<!-- Abstand -->
			<Rectangle Height="5"/>
			<TextBlock Text="{Binding RuntimeText}" FontSize="16"/>
		</StackPanel>

		<Button Grid.Row="0"
			ToolTip="Opens a new save CSV dialog window to select the save location of the file"
			HorizontalAlignment="Right"
			VerticalAlignment="Top"
			Click="ExportCsv_Click"
			Style="{StaticResource CustomButtonStyle}">
			<Button.Content>
				<StackPanel Orientation="Horizontal">
					<fa:ImageAwesome Icon="FileExcelOutline" Width="10" Height="10"/>
					<TextBlock Text="Export CSV" Margin="5,0,0,0"/>
				</StackPanel>
			</Button.Content>
		</Button>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<!-- Chart 1: Grown / Burned -->
			<Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="5">
				<oxy:PlotView x:Name="GrownBurnedPlot"/>
			</Border>

			<!-- Chart 2: Active Trees -->
			<Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
				<oxy:PlotView x:Name="ActiveTreesPlot"/>
			</Border>
		</Grid>
	</Grid>
</Window>
